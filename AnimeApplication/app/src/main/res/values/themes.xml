<resources>
    <!-- App theme with NO window action bar -->
    <style name="Theme.AnimeApplication" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Splash -->
    <style name="Theme.AnimeApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenBackground">@color/splash_bg_purple</item>
        <item name="postSplashScreenTheme">@style/Theme.AnimeApplication</item>
    </style>
</resources>